<h1>New Sponsored Post</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for posts</p>
    <ul>
      <li>Must be funny</li>
      <li>Can't be violent, we have kids here</li>
      <li>If you don't pay, it doesn't post</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@topic, @sponsoredpost] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter Post Title" %>
      </div>
      <div class="form-group">
        <%= f.label :body%>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Entire Body Post"%>
      </div>
      <div class="form-group">
      <%= f.label :price %>
        <%= f.number_field :price, class: 'form-control', placeholder: "Enter price" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
    <%end%>
      </div>
  </div>
